# PRD: 영어 단어 학습 웹 앱 "WordQuest" (가칭)

- **버전**: 0.1 (초안)
- **작성일**: 2026-08-14
- **상태**: 검토 중

---

## 1. 개요 (Overview)

### 1.1 한 줄 요약
성인 학습자가 **게임처럼 재미있게, 과학적으로 효율적으로** 영어 단어를 암기할 수 있는 웹 앱.

### 1.2 문제 정의 (Problem)
- 단어장을 처음부터 끝까지 순서대로 외우는 방식은 지루하고, 앞에서 외운 단어를 금방 잊는다.
- 기존 앱들은 "반복"은 있어도 "재미"가 없어서 며칠 쓰다가 그만두게 된다.
- 단어의 철자·뜻만 외우면 연상 고리가 없어 장기 기억으로 넘어가지 못한다.
- 바쁜 성인은 하루에 긴 시간을 내기 어렵다. 짧은 자투리 시간에 할 수 있어야 한다.

### 1.3 해결 방향 (Solution)
1. **간격 반복(SRS, Spaced Repetition System)**: 망각곡선에 맞춰 "잊기 직전"에 단어를 다시 보여줘 최소 반복으로 장기 기억화.
2. **게임화(Gamification)**: 포인트·레벨·스트릭·퀘스트로 매일 접속할 동기를 부여.
3. **연상 학습**: 이미지·예문·어원·스토리로 단어에 기억 고리를 만들어줌.
4. **AI 활용**: 학습자 수준에 맞는 예문 생성, 연상법(니모닉) 제안, 맞춤 퀴즈 출제.

### 1.4 타깃 사용자 (Target User)
- **주 타깃**: 자기계발/실용 목적으로 영어 어휘를 늘리고 싶은 일반 성인 (20~50대).
- **특징**: 하루 10~20분 정도 투자 가능. 시험보다는 실사용(독해, 회화, 미디어 소비) 목적. 작심삼일 경험이 많아 "지속하게 만드는 장치"가 중요.

### 1.5 사용자 페르소나
| 페르소나 | 설명 | 핵심 니즈 |
|---|---|---|
| 직장인 지민 (32) | 출퇴근 지하철에서 학습. 영어 뉴스/드라마를 자막 없이 보고 싶음 | 모바일 브라우저에서 5~10분 단위 학습, 진도 시각화 |
| 대학원생 현우 (27) | 논문 읽기용 어휘 확장. 효율 중시 | SRS 기반 최소 반복, 커스텀 단어장 |
| 주부 은영 (45) | 자녀와 함께 다시 시작하는 영어. 재미가 최우선 | 쉬운 UI, 게임 요소, 성취감 |

---

## 2. 목표 및 성공 지표 (Goals & Metrics)

### 2.1 제품 목표
- 사용자가 **매일 돌아오게** 만든다 (습관 형성).
- 외운 단어가 **장기 기억으로 정착**되게 한다.

### 2.2 성공 지표 (KPI)
| 지표 | 목표 (출시 3개월) |
|---|---|
| 7일 리텐션 | 30% 이상 |
| 평균 스트릭 길이 | 5일 이상 |
| 일일 학습 완료율 (오늘의 복습 큐 소진) | 60% 이상 |
| 단어 장기 기억률 (30일 후 복습 정답률) | 80% 이상 |
| 1회 세션 평균 시간 | 8~15분 (너무 길면 부담, 너무 짧으면 효과 없음) |

---

## 3. 핵심 기능 (Features)

### 3.1 MVP (1차 출시 범위)

#### F1. 단어장 & 학습 콘텐츠
- **기본 단어장: `영어단어장.csv` (3,167단어)** — 사용자가 보유한 단어장을 필수 포함. 컬럼: 단어, 발음기호, 뜻(한국어), 예문. `convert_csv.py`로 앱 데이터로 변환.
- 각 단어 카드 구성: **철자, 발음기호, 발음 듣기(TTS), 뜻(한국어), 예문 1~3개, 품사, 유의어/반의어**.
- 사용자 커스텀 단어장: 직접 단어 추가/편집/삭제.

#### F2. SRS 기반 복습 엔진 (핵심)
- 알고리즘: **FSRS** (또는 SM-2로 시작 후 개선). 카드마다 난이도·안정도를 추적해 다음 복습 시점 계산.
- "오늘의 학습" = 신규 단어 N개(기본 10, 조절 가능) + 복습 예정 단어.
- 복습 시 자기 평가: `모름 / 애매함 / 알았음 / 쉬움` 4단계 → 다음 복습 간격에 반영.

#### F3. 퀴즈 모드 (다양한 문제 유형)
같은 단어라도 매번 다른 방식으로 물어봐서 지루함 방지 + 다각도 기억 강화:
- 뜻 → 단어 4지선다
- 단어 → 뜻 4지선다
- 예문 빈칸 채우기 (문맥 추론)
- 철자 입력 (스펠링)
- 발음 듣고 단어 고르기 (리스닝)

#### F4. 게임화 시스템
- **XP & 레벨**: 학습·퀴즈 정답으로 XP 획득, 레벨업 연출.
- **스트릭(연속 학습)**: 매일 학습 완료 시 스트릭 유지. 스트릭 보호권(프리즈) 아이템으로 하루 실수 방어.
- **일일 퀘스트**: "오늘 복습 20개 완료", "스펠링 5개 연속 정답" 등 소소한 미션 + 보상.
- **뱃지/업적**: "100단어 마스터", "7일 연속" 등 수집 요소.
- **진도 시각화**: 잔디(히트맵) 캘린더, 암기 단계별 단어 수 그래프 (새싹 → 성장 → 마스터).

#### F5. AI 기능 (MVP 범위)
- **맞춤 예문 생성**: 사용자의 관심 분야(비즈니스, 여행, 영화 등)를 반영한 예문을 AI가 생성.
- **니모닉(연상법) 제안**: 발음 유사 연상, 어원 분해, 짧은 스토리 등 기억 고리를 AI가 제안.
- **틀린 단어 유사 문제 재출제**: 자주 틀리는 단어를 AI가 다른 유형으로 변형 출제.

#### F6. 계정 & 동기화
- 이메일/소셜 로그인 (Google).
- 학습 데이터 서버 저장 → 기기 간 이어서 학습.

### 3.2 2차 이후 (Post-MVP 백로그)
- 이미지 연상 카드 (AI 이미지 생성 또는 큐레이션).
- 발음 평가 (마이크 녹음 → AI 피드백).
- 친구/리그 경쟁 (주간 XP 리더보드).
- 문장 속 단어 수집: 기사/자막에서 모르는 단어 클리핑 → 자동 단어장 생성.
- PWA 오프라인 학습 & 푸시 알림 (복습 시간 리마인드).
- 오답 노트 및 취약 유형 분석 리포트.

### 3.3 명시적 제외 범위 (Non-goals)
- 문법 강의, 회화 커리큘럼 (어휘에 집중).
- 시험(토익 등) 특화 모드 — 추후 단어장 팩으로 확장 가능성만 열어둠.
- 네이티브 모바일 앱 (웹/PWA로 우선 대응).

---

## 4. 사용자 흐름 (Core User Flow)

```
[첫 방문]
회원가입 → 관심 분야·수준 선택 (간단 레벨 테스트 선택 가능)
→ 첫 학습 세트 10단어 추천 → 튜토리얼 학습 → 첫 뱃지 획득

[매일 루틴 - 핵심 루프]
접속 → "오늘의 학습" (복습 M개 + 신규 N개)
→ 카드 학습 (뜻·예문·니모닉 확인, 자기 평가)
→ 퀴즈 (랜덤 유형)
→ 결과: XP 획득, 스트릭 +1, 퀘스트 달성 체크
→ 진도 대시보드 확인 → 종료 (10~15분)
```

**설계 원칙**: 접속 후 3클릭 이내에 학습이 시작되어야 한다. 오늘 할 일이 명확히 하나로 보여야 한다 ("오늘의 학습" 버튼 하나).

---

## 5. 기술 스택 (제안)

| 영역 | 선택 | 이유 |
|---|---|---|
| 프론트엔드 | 순수 HTML/CSS/JavaScript (빌드 도구 없음) | 개발 PC에 Node.js 미설치. 설치 없이 `index.html` 더블클릭으로 즉시 실행 — "각 단계 실행 가능" 원칙에 부합 |
| 데이터 | `convert_csv.py`로 CSV → `data/words.js` 생성 | Python 3.13 보유. 브라우저에서 CSV 직접 로드 시 file:// 제약이 있어 사전 변환 방식 채택 |
| 백엔드 | Node.js (또는 Supabase/Firebase로 시작) | MVP 속도 우선. Supabase면 인증+DB 즉시 해결 |
| DB | PostgreSQL | 학습 이력·SRS 상태 저장 |
| AI | Claude API | 예문 생성, 니모닉, 문제 변형 출제 |
| TTS | Web Speech API (무료) → 추후 고품질 TTS | MVP 비용 절감 |
| 배포 | Vercel / Netlify | 무료 티어로 시작 |

> MVP는 **로컬 우선(localStorage/IndexedDB) + 나중에 계정 동기화** 순서로 개발해도 됨 — 초기엔 서버 없이 핵심 학습 루프를 먼저 검증.

---

## 6. 데이터 모델 (개요)

- **Word**: id, 철자, 발음기호, 뜻[], 품사, 예문[], 난이도, 단어장 id
- **Deck(단어장)**: id, 이름, 설명, 공식/커스텀 여부
- **CardState(사용자별 SRS 상태)**: userId, wordId, 안정도, 난이도, 다음 복습일, 복습 이력[]
- **UserProfile**: XP, 레벨, 스트릭, 뱃지[], 설정(신규 단어 수, 관심 분야)
- **QuizLog**: 문제 유형, 정답 여부, 응답 시간 (취약점 분석용)

---

## 7. 릴리스 계획 (Milestones)

> **원칙: 모든 단계는 끝날 때마다 그 자체로 실행 가능해야 한다** — `index.html`을 브라우저로 열면 바로 동작.

| 단계 | 범위 | 상태 |
|---|---|---|
| M1: 학습 코어 | CSV 단어 3,167개 + 단어 카드 + SRS 엔진 + 퀴즈 4종 + 로컬 저장 | ✅ 완료 |
| M2: 게임화 | XP/레벨/스트릭/퀘스트/잔디 캘린더 | ✅ 완료 |
| M3: AI | 예문 생성, 니모닉, 변형 출제 (Claude API) | ✅ 완료 |
| M4: 동기화/배포 | GitHub Gist 백업·자동 동기화 + GitHub Pages 배포 (1인 사용에 맞춰 로그인 대신 토큰 방식 채택) | ✅ 완료 |
| 베타 | 지인 10~20명 테스트 → 리텐션 지표 확인 | 예정 |

---

## 8. 리스크 및 대응

| 리스크 | 대응 |
|---|---|
| 게임화가 초반 흥미만 주고 학습 효과로 이어지지 않음 | 보상을 "복습 완료"에 연동 — 찍어서 넘기면 XP 미지급 (오답 시 XP 감소 없음, 벌점 금지) |
| SRS 복습 큐가 쌓여서 부담 → 이탈 | 하루 복습 상한 설정, 밀린 복습 자동 분산, "오늘은 5분만" 라이트 모드 |
| AI API 비용 | 예문/니모닉은 생성 후 캐싱해 재사용, 기본 콘텐츠는 사전 생성 |
| 콘텐츠(단어장) 구축 비용 | 공개 빈도 리스트(예: NGSL) 기반 + AI로 예문·뜻 사전 생성 후 검수 |

---

## 9. 열린 질문 (Open Questions)

1. 레벨 테스트를 MVP에 넣을지, 자가 선택(초/중/고급)으로 시작할지?
2. 신규 단어 기본값 — 하루 10개가 적절한가? (베타에서 A/B)
3. 한국어 뜻 우선 vs 영영 정의 병기 — 타깃(성인 일반)에겐 한국어 우선이 맞아 보이나 옵션 제공 여부?
4. 수익 모델 (추후): 무료 + 프리미엄(AI 기능 무제한, 고급 단어장)?
